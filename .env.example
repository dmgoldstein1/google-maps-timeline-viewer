# Generated by GitHub Copilot using Claude Sonnet 4.5
# Copy this file to .env and fill in your values
# This template provides example configuration for the Google Maps Timeline Viewer

# Google Maps API Key (alternative to Docker secrets)
# Obtain from https://console.cloud.google.com/
GOOGLE_MAPS_API_KEY=

# Path to API key file (used in Docker with secrets)
GOOGLE_MAPS_API_KEY_FILE=/run/secrets/google_api_key

# Cache expiration in days (default: 180 days / 6 months)
CACHE_TTL_DAYS=180

# Daily API quota limit (Google Places API free tier: 5000 requests/day)
DAILY_API_QUOTA=5000

# Maximum concurrent fetch workers for prefetch script
MAX_CONCURRENT_FETCHES=5

# Minimum delay between API requests per worker (milliseconds)
API_REQUEST_DELAY_MS=200

# Server configuration
PORT=3000
NODE_ENV=production

# Logging level (trace, debug, info, warn, error, fatal)
LOG_LEVEL=info

# Data directory (where SQLite database and photos are stored)
DATA_DIR=/data

# Photo sizes to generate (comma-separated pixel widths)
PHOTO_SIZES=150,400,800,1200

# Image quality settings
WEBP_QUALITY=85
JPEG_QUALITY=80
